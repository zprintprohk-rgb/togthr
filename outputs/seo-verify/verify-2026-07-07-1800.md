# togthr.life Daily SEO Verify — 2026-07-07 18:00

**Verdict: FAIL** — 32/40 PASS, 8/40 FAIL (P0 regression on 7/7 post)

## Scope
- Base URL: `https://togthr.life` (followed redirect to `/en` — DNS OK)
- Slugs × Locales matrix: **5 × 8 = 40 URLs**
- Method: HTTP 200 + body grep `"This post is part of the Togthr Blog"` (fallback wrapper) + body keyword presence

## Results

| Slug                                          | EN | zh-CN | zh-TW | JA | KO | DE | FR | ES |
| --------------------------------------------- | -- | ----- | ----- | -- | -- | -- | -- | -- |
| why-we-built-a-pet-that-grows-with-you        | OK | OK    | OK    | OK | OK | OK | OK | OK |
| three-small-rituals-for-couples-who-live-apart| OK | OK    | OK    | OK | OK | OK | OK | OK |
| what-your-virtual-pet-notices                 | OK | OK    | OK    | OK | OK | OK | OK | OK |
| virtual-companion-pet-loss-comfort            | OK | OK    | OK    | OK | OK | OK | OK | OK |
| **two-minute-daily-check-in-ai-companion**    | **FAIL** | **FAIL** | **FAIL** | **FAIL** | **FAIL** | **FAIL** | **FAIL** | **FAIL** |

## Root cause (7/7 post FAIL)

**The 2026-07-07 daily post `two-minute-daily-check-in-ai-companion` has NO per-slug `page.tsx` file.**
All 8 locale URLs fall through to `[slug]/page.tsx` catch-all which renders only the fallback wrapper text.

### Evidence
1. **Source code missing** — `src/app/[locale]/blog/two-minute-daily-check-in-ai-companion/` directory does not exist:
   ```
   glob F:\CloudDreamerApp\togthr\src\app\[locale]\blog\two-minute-daily-check-in-ai-companion/** → No files found
   ```
2. **Git history confirms** — `git log --all -- 'src/app/[locale]/blog/two-minute-daily-check-in-ai-companion/*'` returns empty:
   ```
   (no output)
   ```
   The post was added in commit `fc6a7ed` (metadata only in `src/lib/blog-posts.ts`), and **was missed by the P0-1 fix commit `c172548`** ("per-slug real content pages for 4 blog posts") — which only added per-slug page.tsx for launch + 7/4 + 7/5 + 7/6.

3. **Production HTML inspection** (`/en/blog/two-minute-daily-check-in-ai-companion`, 80955 bytes):
   - `<div class="prose prose-invert max-w-none"><p class="text-zinc-300">This post is part of the Togthr Blog. Read more posts in this series, or try Togthr free — a small robot in your device, always here.</p>` ← fallback wrapper rendered as visible prose body
   - Second occurrence is in `self.__next_f.push(...)` RSC payload (same content)
   - Body keyword counts (e.g. "check-in" 91×) come from title / meta / JSON-LD, not real prose

4. **Reference for the broken path** (`src/app/[locale]/blog/[slug]/page.tsx:217-220`):
   ```tsx
   <div className="prose prose-invert max-w-none">
     <p className="text-zinc-300">
       This post is part of the Togthr Blog. Read more posts in this series, or try
       Togthr free — a small robot in your device, always here.
     </p>
   ```

## Why other 7 locales of 7/6 post also "pass" verify
Even though `virtual-companion-pet-loss-comfort/page.tsx` only ships EN body (per the TODO comment in the file), it is per-locale-aware via `BODIES[loc] ?? BODIES.en` fallback in the same file — so each locale gets the **EN content** rendered under its own URL, not the generic wrapper. That's why all 8 of 7/6 pass.

The 7/7 post has no per-slug file at all → goes straight to the catch-all wrapper.

## Impact
- 8 URLs × 7 locales + EN = **8 thin-content / doorway pages** live in production for the 7/7 post
- Same class of bug as the P0 caught on 7/6 18:00 (commit c172548's stated goal), but **only partially fixed** — 7/7 daily was added later the same day (commit fc6a7ed) AFTER the P0-1 sweep
- Likely user-facing impact: low (still 200 OK, has title/cover), but SEO impact: high (Google sees 8 near-identical wrapper pages + diluted internal-link signals)

## Recommendation (P0 — fix in cron next iteration, not in this verify run)
1. Create `src/app/[locale]/blog/two-minute-daily-check-in-ai-companion/page.tsx` with BODIES for all 8 locales (or at minimum BODIES.en + 7-locale fallback, matching the 7/6 pattern)
2. Build + commit + push to trigger CF Pages deploy
3. Re-run this verify cron to confirm 40/40 PASS
4. Long-term: cron prompt `togthr-daily-content` should HARD-REQUIRE the per-slug page.tsx to exist BEFORE merging the metadata entry into `blog-posts.ts` (this requirement is documented but evidently not enforced at merge time)

## Files
- Script: `outputs/seo-verify/verify-2026-07-07-1800.py`
- JSON dump: `outputs/seo-verify/verify-2026-07-07-1803.json`
- Captured HTML: `outputs/seo-verify/post-7-7-en.html` (debug of FAIL row)