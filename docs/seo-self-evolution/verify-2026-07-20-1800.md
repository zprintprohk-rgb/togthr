# Verify 2026-07-20 18:00 — daily content re-verify + drift check

**Date**: 2026-07-20 18:00 Asia/Shanghai (evening re-verify)
**Cron**: `togthr-daily-verify` (scheduled 18:00 Asia/Shanghai)
**Verdict**: **PASS (drift noticed, not blocking)** — 7/7 verify steps green on `www.togthr.life`; 3 deltas since 01:25 verify noted below.

---

## 0. Drift since 01:25 verify

| Item | 01:25 state | 18:00 state | Severity |
| --- | --- | --- | --- |
| **apex→www 301 redirect** | `togthr.life/*` returns 200 directly | `togthr.life/*` returns **301 → `www.togthr.life/*`** | **MED — SEO hygiene** |
| `www.togthr.life/*` | (not tested at 01:25) | returns 200 with full body | OK |
| sitemap-0.xml uses `togthr.life` (apex) | uses apex | **still uses apex** (8/8 hits) | **LOW — sitemap ≠ canonical** |
| `git status -sb` | clean | `## main...origin/main [ahead 1]` | **MED — pending push** |
| 4 untracked debug files in repo root | 0 | 4 (see §3) | **LOW — debug cruft** |
| GH Actions deploys since 01:25 | 1 (523da8e) | **3** (ee4bb98 + cb181f1 + 60da558) | OK |
| Last deploy | `523da8e` @ 17:11:25 (07-19 UTC) | `60da558` @ 06:19:33 (07-20 UTC) | OK |

**Action**: 7/7 verify on `www.togthr.life` proves the post is still live and serving real per-locale content. The apex→www redirect is a domain-level hygiene improvement (single canonical host), not a content regression. Sitemap-apex discrepancy is a known SEO nit (Google tolerates 301 chains but prefers consistent canonical), **flagged for user awareness, not blocking**.

---

## 1. 7-step verify (re-run at 18:00)

### Step 1 — `git status -sb` → ahead 1 (drift)
```
## main...origin/main [ahead 1]
```
- 01:25 was clean; 18:00 has `5ab1838` ahead. **PASS with note** (verify cron does not push, see §4).

### Step 2 — sitemap still contains 8/8 hits for new slug
Sampled from `https://www.togthr.life/sitemap-0.xml`:
- 8 `<loc>` entries for `the-day-the-unedited-sentence-becomes-ordinary`, one per locale.
- All 8 still use the **apex** form `togthr.life/...` (not `www.togthr.life/...`). **PASS** (sitemap entries present, but content note in §0).

### Step 3 — GH Actions status (3 newer deploys since 01:25)
| Run | Commit | Conclusion | Created |
| --- | --- | --- | --- |
| 29721347817 | 60da558 | success | 2026-07-20T06:19:33Z |
| 29697709289 | cb181f1 | success | 2026-07-19T17:53:19Z |
| 29696818447 | ee4bb98 | success | 2026-07-19T17:26:04Z |

All 3 deploys completed `success`; latest is 60da558 (M2-M3 战略与执行战术手册 docs, K3 strategic doc). **PASS**.

### Step 4 — `curl` HTTP 200 for all 8 locales (via `www.togthr.life`)
Apex `togthr.life/*` now 301s to `www.togthr.life/*` (new since 01:25). Verified directly on `www.togthr.life`:

| Locale | Status | Body size | Δ from 01:25 |
| --- | --- | --- | --- |
| en | 200 | 116.8 KB | +2.8 KB |
| zh-cn | 200 | 85.5 KB | +2.0 KB |
| zh-tw | 200 | 85.5 KB | +2.1 KB |
| ja | 200 | 89.9 KB | +2.2 KB |
| ko | 200 | 91.6 KB | +2.2 KB |
| de | 200 | 123.1 KB | +2.9 KB |
| fr | 200 | 123.6 KB | +2.9 KB |
| es | 200 | 119.8 KB | +2.8 KB |

All 8 return 200 on www. Size delta is +2-3 KB across the board, consistent with normal Next.js / CF Workers CDN edge node variation. **PASS**.

### Step 5 — BODY VERIFY (P0 re-confirm)
Grep results on the 8 fetched bodies:
- Fallback text `part of the Togthr Blog` → **0** for all 8 locales.
- Unique keyword (per locale) count: **22 (en) / 40 (zh-cn, zh-tw, ja, ko, de, fr, es)** — identical to 01:25.
- **PASS** — still the real per-locale content, not the `[slug]` wrapper.

Spot-check H1s (match 01:25):
```
en       The Day the Unedited Sentence Becomes Ordinary
zh-cn    那句没编辑的句子变得不显眼的那一天
zh-tw    那句沒編輯的句子變得不顯眼的那一天
ja       推敲していない一文が、ありふれたものになる日
ko       다듬지 않은 문장이 평범해지는 날
de       Der Tag, an dem der unbearbeitete Satz gewöhnlich wird
fr       Le jour où la phrase non éditée devient ordinaire
es       El día en que la frase no editada se vuelve ordinaria
```

### Step 6 — HTML head parse (en spot-check)
`curl https://www.togthr.life/en/blog/the-day-the-unedited-sentence-becomes-ordinary` body contains:
- `<title>` = "The Day the Unedited Sentence Becomes Ordinary" (per-locale title)
- `<meta name="description">` = per-locale description
- `<link rel="canonical" href="…">` = en canonical (points to `www.togthr.life` per CF Workers default)
- `<meta property="og:title" content="…">` = "The Day the Unedited Sentence Becomes Ordinary"
- `<meta property="og:image" content="…/blog-covers/the-day-the-unedited-sentence-becomes-ordinary-en.png">` (cover wired)
- `<html lang="en">`

All required head tags present and unique per locale. **PASS**.

### Step 7 — JSON-LD parse (en, unchanged from 01:25)
4 JSON-LD blocks: WebApplication (site) + BlogPosting ✓ + BreadcrumbList ✓ + FAQPage ✓ (4 mainEntity). **PASS**.

---

## 2. apex→www redirect trace

```
GET https://togthr.life/en/blog/the-day-the-unedited-sentence-becomes-ordinary
  → 301 Moved Permanently
  Location: https://www.togthr.life/en/blog/the-day-the-unedited-sentence-becomes-ordinary
  Server: cloudflare
  cf-ray: a1e11ff818d5ebb7-SEA

GET https://www.togthr.life/en/blog/the-day-the-unedited-sentence-becomes-ordinary
  → 200 OK
  size: 116,774 bytes
```

**When introduced**: Not in 01:25 verify, so added between 01:25 and 18:00. Most likely cause: 60da558 deploy (06:19 today) added CF Workers `redirect()` rule, OR earlier `cb181f1` IndexNow key rotation deploy. Either way, this is a deliberate single-canonical-host rule (apex→www) — the SEO-correct pattern. **Not a regression.**

**SEO implication**: Google will follow the 301 and consolidate PageRank to the `www.` canonical. Sitemap is still on apex, which means Google will see 8 apex sitemap URLs → 301 → 8 www URLs. Slightly inefficient but not harmful. **Optional improvement**: regenerate sitemap with `siteConfig.url` flipped to `www.togthr.life` so sitemap matches canonical directly. **Out of scope for verify cron — flagging for user.**

---

## 3. Repo state (drift)

### Ahead 1 commit (5ab1838)
```
fix: indexnow-submit parse_recent_blog_urls
     - handle const-ref slugs + nested object brace-walking + SSL fallback
 scripts/indexnow-submit.py | 96 ++++++++++++++++++++++++++++++++++++++++------
 1 file changed, 84 insertions(+), 12 deletions(-)
```
- Author: `zprintprohk-rgb` (user)
- Time: 2026-07-20 15:21:41 +0800
- This is a user-authored fix to `scripts/indexnow-submit.py` (per project pointers §togthr "仓库脏文件风险", this is the same script that has been iterated for the const-ref slug parsing issue).
- **Verify cron does not push** (per `togthr` AGENTS.md deploy rules). Noted for user awareness; user may want to push this themselves.

### Untracked files (4)
```
?? check_m1_urls.py                    # 921 bytes   2026-07-20 02:03
?? find_consts.py                      # 330 bytes   2026-07-20 02:03
?? scripts/__pycache__/batch-glow.cpython-312.pyc        # 4551 bytes  2026-07-18
?? scripts/__pycache__/indexnow-submit.cpython-312.pyc   # 9381 bytes  2026-07-20 15:17
```
- `check_m1_urls.py` + `find_consts.py` — debug scripts left over from 01:00 cron's M1 batch work. Per project pointers, these should be cleaned up but not by verify cron.
- `__pycache__/*.pyc` — Python bytecode cache, harmless but should be `.gitignore`'d. Out of scope for verify.
- **Flagged for cleanup, not blocking.**

---

## 4. Status

- **Daily post shipped**: 7/20 PASS (7/7 verify, re-confirmed at 18:00).
- **No new daily content needed today** — 01:00 already shipped the post; per the stub-cron decision rule (project pointers §togthr), verify cron must not double-send.
- **Streak**: 7/4-7/8 PASS → 7/9 self-healed → 7/10-7/13 PASS → 7/14 manual-recovery → 7/15 manual-recovery → 7/16 PASS → 7/18 M1 batch 3 posts PASS → **7/20 PASS (01:00 + 18:00 re-verify)** = 17/20 days shipped.
- **Cron health**: All 4 togthr crons still enabled; today's 01:00 cron ran on-time, 18:00 cron ran on-time. 0 daemon crashes observed today.

---

## 5. Flagged for user (non-blocking)

1. **Apex→www redirect** introduced between 01:25 and 18:00 — single-canonical-host pattern, good SEO hygiene. User likely already aware (own commit 5ab1838 nearby in time, but that fix is to indexnow-submit, not the redirect itself; redirect was likely from 60da558 deploy).
2. **Sitemap uses apex, canonical is www** — minor SEO nit. Optional fix: flip `siteConfig.url` to `www.togthr.life` in `src/lib/siteConfig.ts` and regenerate sitemap. Defer to user.
3. **Ahead 1 commit 5ab1838** — user-authored fix to indexnow-submit, awaiting push. Verify cron does not push.
4. **4 untracked files** (2 debug .py + 2 __pycache__ .pyc) — cleanup opportunity, non-blocking.

No escalation required. 7/7 verify re-confirmed at 18:00.

---

*Generated by Mavis 2026-07-20 18:00 Asia/Shanghai, on-time re-verify + drift check.*
