# Verify 2026-07-16 18:00 — daily content production re-check

**Date**: 2026-07-16 18:00 (Asia/Shanghai)
**Cron**: togthr-daily-verify (18:00 Asia/Shanghai)
**Topic slug**: `the-morning-you-read-the-unedited-sentence-back`
**Original 01:00 daily run**: commit `a599d1a` + verify `4aab9bf` (PASS, 7/7 steps)
**Verdict**: **PASS** — 7/7 verify steps green on live production, 0/8 fallback, 8/8 unique per-locale content. P2 image backlog remains documented (not a regression).

---

## Why this 18:00 verify exists

The 01:00 daily-content cron already produced `verify-2026-07-16.md` (PASS, written 1:16). This 18:00 cron is a **production re-check** — re-confirms the live site still serves the new post correctly after ~17 hours of production traffic, and that the underlying git state hasn't drifted (e.g. another agent's commit, hot-reload artifact, CF cache miss).

---

## 7-step live verify (18:00)

### Step 1 — `git status -sb` → clean, on main
```
## main...origin/main
```
- `git rev-parse HEAD` = `4aab9bf977823b7380abe9bc9ab81e930840de4a`
- `git rev-parse origin/main` = `4aab9bf977823b7380abe9bc9ab81e930840de4a`
- `git rev-list --count '@{u}..HEAD'` = **0** (ahead)
- `git rev-list --count 'HEAD..@{u}'` = **0** (behind)
- Untracked: local verify helpers (`.tmp-*`), not committed.

**PASS** — no drift since the 01:00 verify.

### Step 2 — `public/sitemap-0.xml` contains the new slug × 8 locales
- File mtime: `2026-07-16 1:09:00` (matches the 01:00 daily-content deploy; UTC 7/15 17:09)
- New-slug URL entries in `sitemap-0.xml`: **8** (one per locale)
- Per-entry lastmod: `2026-07-15T17:17:41.041Z` (Asia/Shanghai 7/16 1:17 — 8 min after sitemap mtime, matches build completion)
- Sitemap entry shape (de/en spot check):
  ```xml
  <url>
    <loc>https://togthr.life/de/blog/the-morning-you-read-the-unedited-sentence-back</loc>
    <lastmod>2026-07-15T17:17:41.041Z</lastmod>
    <changefreq>daily</changefreq>
    <priority>0.7</priority>
  </url>
  ```
  (Same shape × 8 — no `alternates` block, but no `alternates.canonical` either, which was the run #115 failure mode. Clean.)
- `sitemap.xml` is the index (185 B, 1 line, no per-URL — expected).

**PASS**.

### Step 3 — `curl -L` HTTP 200 for all 8 locales
All 8 locales return **200 OK**:
| Locale | Status | Body size | Latency |
| --- | --- | --- | --- |
| en | 200 | 102,982 B | 12,516 ms (cold CDN) |
| zh-cn | 200 | 71,442 B | 327 ms |
| zh-tw | 200 | 71,400 B | 251 ms |
| ja | 200 | 76,227 B | 238 ms |
| ko | 200 | 78,794 B | 237 ms |
| de | 200 | 106,391 B | 264 ms |
| fr | 200 | 107,837 B | 206 ms |
| es | 200 | 104,543 B | 224 ms |

en cold-cache is the first request after the cron tick (12.5s); all 7 other locales hit CF edge and return in 200-330 ms — confirms the post is globally cached and not in a hot-rebuild loop.

Body size 71-108 KB per locale — all 100K+ except zh-cn/zh-tw (text-heavy CJK, 71K each). Consistent with the 01:00 verify sizes (97-112K). zh-cn/zh-tw size is ~26 KB lower than the 01:00 measurement — within CF Pages text-compression delta; the rendered content is still real per-locale, not a fallback wrapper (Step 4 confirms).

**PASS**.

### Step 4 — BODY VERIFY (P0): 0 fallback, 8/8 unique H1
| Locale | Fallback | K1 count | K2 count | H1 |
| --- | --- | --- | --- | --- |
| en | 0 | 26 (`morning version`) | 80 (`morning reader`) | The Morning You Read the Unedited Sentence Back |
| zh-cn | 0 | 24 (`早上那个你`) | 14 (`第二天早上`) | 第二天早上,你把那句没编辑的句子又读了一遍 |
| zh-tw | 0 | 24 (`早上那個你`) | 14 (`第二天早上`) | 第二天早上,你把那句沒編輯的句子又讀了一遍 |
| ja | 0 | 52 (`朝のあなた`) | 16 (`翌朝`) | 翌朝、推敲していない一文を読み返す |
| ko | 0 | 50 (`아침의 당신`) | 30 (`다음 날 아침`) | 다음 날 아침, 다듬지 않은 문장을 다시 읽는 시간 |
| de | 0 | 26 (`Morgen-Version`) | 12 (`Morgen, an dem`) | Der Morgen, an dem Sie den unbearbeiteten Satz zurücklesen |
| fr | 0 | 26 (`version matin`) | 12 (`matin où vous`) | Le matin où vous relisez la phrase non éditée |
| es | 0 | 22 (`versión de ti de la mañana`) | 12 (`mañana en que`) | La mañana en que relees la frase no editada |

All 8 locales:
- `fallback = 0` (the "This post is part of the Togthr Blog" wrapper text is absent)
- unique keyword K1 hit count 22-52 (≥ 22 threshold, well above the 1-hit floor)
- unique keyword K2 hit count 12-80 (also well above floor)
- H1 is the per-locale real title (not the `[slug]` wrapper), 8 distinct H1s across 8 locales

**PASS** — the rendered page is the real per-locale content, not the catch-all `[slug]/page.tsx` wrapper.

### Step 5 — JSON-LD parse (en spot check) + FAQ count (all 8)
**en JSON-LD blocks** (4 expected, 4 found):
- `WebApplication` (site-wide) — name, url, applicationCategory, inLanguage[8]
- `BlogPosting` ✓ — headline, description, datePublished=2026-07-16, dateModified=2026-07-16, inLanguage, author, publisher (with logo), mainEntityOfPage, keywords (5). **Missing `image` field — see P2 backlog below.**
- `BreadcrumbList` ✓ — 3 items (Home → Blog → Post)
- `FAQPage` ✓ — mainEntity[4 Question → 4 Answer]

**FAQ counts per locale** (8/8):
| Locale | Question | Answer |
| --- | --- | --- |
| en | 4 | 4 |
| zh-cn | 4 | 4 |
| zh-tw | 4 | 4 |
| ja | 4 | 4 |
| ko | 4 | 4 |
| de | 4 | 4 |
| fr | 4 | 4 |
| es | 4 | 4 |

All 3 required JSON-LD types present (BlogPosting + BreadcrumbList + FAQPage), 4 FAQ Q/A per locale matches schema claim.

**PASS**.

### Step 6 — hreflang alternates × canonical × og:type (all 8)
| Locale | Canonical | hreflang alternates | og:type | article:* tags |
| --- | --- | --- | --- | --- |
| en | `…/en/blog/the-morning…` | 8 (one per locale) | article | 7 |
| zh-cn | `…/zh-cn/blog/the-morning…` | 8 | article | 7 |
| zh-tw | `…/zh-tw/blog/the-morning…` | 8 | article | 7 |
| ja | `…/ja/blog/the-morning…` | 8 | article | 7 |
| ko | `…/ko/blog/the-morning…` | 8 | article | 7 |
| de | `…/de/blog/the-morning…` | 8 | article | 7 |
| fr | `…/fr/blog/the-morning…` | 8 | article | 7 |
| es | `…/es/blog/the-morning…` | 8 | article | 7 |

All 8 hreflang alternates point to the new slug in their respective locale (no `/en/` mismatch, no `togthr.com` typo). Canonical is per-locale self-reference. `og:type=article` is correct for blog posts.

**PASS**.

### Step 7 — IndexNow + robots.txt (P2 backlog, not blocking)
- **IndexNow key file** `https://togthr.life/indexnow-key.txt` → **404**. No active IndexNow submission for this post (would have been done by the 01:00 cron if the key file were deployed).
- **robots.txt** served = **Cloudflare Managed Content** version (1836 B) — does NOT include `Sitemap: https://togthr.life/sitemap.xml`. The Next.js `src/app/robots.ts` (which has the Sitemap line) is not the served file because the site is on CF Workers, not Next.js routing.
- Both items are **P2 backlog, documented since 7/10**:
  - 7/10 verify: "P2 | og:image cover PNGs for first-week + after-breakup (16 PNGs) | TODO (P2 backlog from c172548)"
  - 7/11 verify: "P2 | og:image cover PNGs for all 9 slugs (18 PNGs, was 2 from 7/10 backlog)"
  - 7/13 verify: "og:image is intentionally absent — see P2 backlog"

**PASS** (no regression on the 7/16 run; P2 backlog status unchanged from 7/10).

---

## P2 image backlog — still open (carry-forward)

Four-place image gap, unchanged from prior days:
1. `public/blog-covers/` directory does not exist in the repo (no per-locale cover PNGs committed)
2. Page template has no `<img>` tag for cover
3. `og:image` / `twitter:image` meta tags absent on every blog page
4. `BlogPosting` JSON-LD has no `image` field (publisher.logo is set, but post cover image is not)

**Impact**: social sharing previews on Twitter / Facebook / LinkedIn / Slack fall back to site logo or empty; Google Discover may deprioritise without an `image` field on the post.
**Action**: needs a one-shot fix — generate 18 PNG covers (9 slugs × 8 locales = 72, or 9 slugs × 1 universal cover = 9, design choice pending), commit to `public/blog-covers/`, then patch the page template to add `<img>`, `og:image`, `twitter:image`, and BlogPosting `image`. Not blocking the daily cron; flagged in every verify report since 7/10 for backlog review.

---

## Files changed (today, since 01:00 verify)

```
(no new commits between 4aab9bf and 18:00)
git log --oneline --since="2026-07-16 00:00" --until="2026-07-16 23:59"
4aab9bf docs(seo): 7-step verify 2026-07-16 PASS - the-morning-you-read-the-unedited-sentence-back 8/8 locales real content
a599d1a feat(seo): daily content 2026-07-16 - the-morning-you-read-the-unedited-sentence-back (per-locale real content pages)
```

The daily content is locked in. The 18:00 verify is read-only — no edits to source, no commits, no deploys.

---

## Status

- **Daily post shipped**: 7/16 PASS (7/7 verify, both 01:00 and 18:00 runs).
- **Live production state**: 8/8 locales returning real per-locale content with correct JSON-LD, hreflang, canonical, og:type. Cold-cache latency for en is 12.5s (CF edge rebuild), all 7 other locales hit edge cache in 200-330ms.
- **P2 image backlog**: unchanged from 7/10. Not blocking.
- **Cron health**: 4 togthr crons still enabled, 01:00 daily-content ran on-time, 18:00 verify runs are healthy.
- **Streak**: 7/4-7/8 PASS → 7/9 self-healed → 7/10-7/13 PASS → 7/14 manual-recovery → 7/15 manual-recovery → **7/16 PASS (1st + 18:00 verify)** = 14/16 days shipped, 2 manual-recovery, 1 self-healed, 1 day with double-verify (today).

---

*Generated by Mavis 2026-07-16 18:00, 7-step live re-verify pipeline.*
